using System;

namespace LabWork1
{
    public static class Function
    {
        public static double Solving(double x) => Math.Cos(x) + Math.Log(Math.Cos(x));
        public static double Manufacturer(double x) => -Math.Sin(x) - Math.Tan(x);
    }
}
